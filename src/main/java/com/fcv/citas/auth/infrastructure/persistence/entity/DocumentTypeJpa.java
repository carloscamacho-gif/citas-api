package com.fcv.citas.auth.infrastructure.persistence.entity;

/** Refleja com.fcv.citas.auth.domain.model.DocumentType a nivel de persistencia. */
public enum DocumentTypeJpa {
    CC,
    CE,
    TI,
    PASSPORT
}
